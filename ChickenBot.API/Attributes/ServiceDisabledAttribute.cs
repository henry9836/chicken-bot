namespace ChickenBot.API.Attributes
{
    /// <summary>
    /// Marks a service as disabled, so it will not be loaded or executed
    /// </summary>
    public sealed class ServiceDisabledAttribute : Attribute
    {
    }
}
